# 推荐实施顺序

1. 先完成任务 1。它确定精确的 callable 边界并完成最小的非空垂直切片，避免与 SQL 量化或 PostgreSQL `any` 函数冲突。
2. 接着完成任务 2，使新聚合进入既有 `SqlExpr` 布尔组合路径，再扩展边界行为。
3. 之后完成任务 3。空集合和无值情况不能直接继承 `orExpr(emptyList())`，需要独立策略与验证。
4. 最后完成任务 4。文档应描述已测试的行为，广泛验证也应在 compiler/runtime 语义稳定后进行。

在任务 1 至 3 取得 compiler testData 证据前，不应合入公开文档中的功能宣称。
