import {NgDocPage} from '@ng-doc/core';
import MappingCategory from "../ng-doc.category";

const CascadeMappingPage: NgDocPage = {
    title: `级联映射`,
    mdFile: './index.md',
    route: 'cascade-mapping',
    category: MappingCategory,
    order: 10
};

export default CascadeMappingPage;
